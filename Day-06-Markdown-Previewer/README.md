# Day 6: Live Markdown Previewer

## Description
A real-time editor that converts Markdown text into HTML. 
It uses the `marked.js` library for parsing.

## Features
- Split-screen layout (Editor vs Preview).
- Live updates as you type.
- Syntax highlighting for code blocks.

## How to Run
Simply open `index.html` in any web browser.